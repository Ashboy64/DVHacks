<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:divider="@android:color/transparent"
    android:orientation="vertical"


    android:paddingVertical="10dp"
    android:paddingLeft="40dp"
    android:paddingRight="40dp">

    <TextView
        android:id="@+id/textView"
        android:layout_width="163dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="@font/dylan_xbold"
        android:lineSpacingExtra="0dp"
        android:text="DATE OF TIME"
        android:textColor="#F5203A" />

    <TextView
        android:id="@+id/message_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="@font/dylan_normal"
        android:lineSpacingExtra="0dp"
        android:padding="0dp"
        android:paddingHorizontal="10dp"
        android:paddingVertical="20dp"
        android:text="The president declared a state of national emergency."
        android:textColor="#474747"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="222dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="@font/dylan_xbold"
        android:text="MOOD DETECTED: HAPPY"
        android:textColor="#ACACAC"
        android:textSize="12sp" />

</LinearLayout>